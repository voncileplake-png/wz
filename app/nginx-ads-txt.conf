# Nginx 服务器重定向配置
# 将以下配置添加到您的 Nginx server 块中
# 注意：如果使用 Next.js，通常不需要此配置，因为已使用 API 路由处理

server {
    # ... 其他配置 ...
    
    location ~ /ads.txt {
        return 301 https://srv.adstxtmanager.com/19390/www.hotelcorporatecodes.com;
    }
    
    # ... 其他配置 ...
}
